# Computer-use (CUA) experience-collection package for the OSWorld benchmark.
#
# Self-contained sub-package beside experience/rollouts.py. It carries the
# multimodal, multi-turn desktop-agent path (per-turn screenshots threaded back
# into the message log) that the core text rollout does not handle, so the
# validated experience/rollouts.py stays untouched.
