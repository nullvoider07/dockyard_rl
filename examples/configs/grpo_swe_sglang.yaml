# GRPO configuration for SWE-bench coding agent training (SGLang backend).
#
# SGLang counterpart of grpo_swe.yaml — identical except the generation block
# uses backend: sglang + sglang_cfg. Run on the SGLang ubuntu-swe image
# (nullvoider/ubuntu-swe-sglang:latest); the vLLM recipe runs on the default
# vLLM image. Inherits all defaults from the base config.
#
# Topology: non-collocated, async GRPO, SGLang async engine.
# Target hardware: 4 nodes × 8 GPUs (3 trainer + 1 inference).
# Model: Qwen/Qwen2.5-7B-Instruct (adjust policy.model_name as needed).
#
# Usage:
#   python3 examples/run_grpo_swe.py \
#     --config examples/configs/grpo_swe_sglang.yaml \
#     cluster.gpus_per_node=8 \
#     cluster.num_nodes=4

grpo:
  num_prompts_per_step: 16           # prompts pulled from replay buffer per step
  num_generations_per_prompt: 8      # rollouts per prompt (RL group size)
  max_rollout_turns: 1               # single-shot: agent emits one patch, then scored
  max_num_epochs: 1
  max_num_steps: 2000
  normalize_rewards: true
  use_leave_one_out_baseline: true
  val_period: 50
  val_at_start: true
  val_at_end: true
  max_val_samples: 64
  val_batch_size: 8
  seed: 42
  use_dynamic_sampling: false
  dynamic_sampling_max_gen_batches: 10
  batch_multiplier: 1
  overlong_filtering: true           # zero reward for truncated responses
  seq_logprob_error_threshold: 1.5

  adv_estimator:
    name: "grpo"
    normalize_rewards: ${grpo.normalize_rewards}
    use_leave_one_out_baseline: ${grpo.use_leave_one_out_baseline}
    minus_baseline: true

  reward_shaping:
    enabled: true
    overlong_buffer_length: 256
    overlong_buffer_penalty: 1.0
    max_response_length: ${policy.max_total_sequence_length}
    stop_properly_penalty_coef: null

  reward_scaling:
    enabled: false
    source_min: 0.0
    source_max: 1.0
    target_min: 0.0
    target_max: 1.0

  async_grpo:
    enabled: true
    max_trajectory_age_steps: 2      # trajectories stay valid for 2 optimizer steps
    in_flight_weight_updates: true   # vLLM async engine: update weights while generating
    recompute_kv_cache_after_weight_updates: true

loss_fn:
  reference_policy_kl_penalty: 0.01
  reference_policy_kl_type: "k3"
  kl_input_clamp_value: 20.0
  kl_output_clamp_value: 10.0
  ratio_clip_min: 0.2
  ratio_clip_max: 0.2
  ratio_clip_c: null
  use_on_policy_kl_approximation: false
  # async GRPO requires IS correction — must be true when async_grpo.enabled=true
  use_importance_sampling_correction: true
  truncated_importance_sampling_type: null
  truncated_importance_sampling_ratio: null
  truncated_importance_sampling_ratio_min: null
  sequence_level_importance_ratios: false
  token_level_loss: true
  force_on_policy_ratio: false
  use_kl_in_reward: false

checkpointing:
  enabled: true
  checkpoint_dir: "results/grpo-swe"
  metric_name: "val:accuracy"
  higher_is_better: true
  keep_top_k: 3
  save_period: 50
  checkpoint_must_save_by: null
  model_save_format: "safetensors"
  save_consolidated: false
  save_optimizer: true

policy:
  model_name: "Qwen/Qwen2.5-7B-Instruct"
  tokenizer:
    name: ${policy.model_name}
    chat_template: null              # use model's default chat template
    chat_template_kwargs: null
  hf_config_overrides: {}
  train_global_batch_size: 128       # num_prompts_per_step * num_generations_per_prompt
  train_micro_batch_size: 2
  logprob_batch_size: ${policy.train_micro_batch_size}
  max_total_sequence_length: 8192    # prompt + max agent response
  precision: "bfloat16"
  logprob_chunk_size: null
  offload_optimizer_for_logprob: true  # non-collocated: offload before logprob pass
  max_grad_norm: 1.0
  make_sequence_length_divisible_by: ${policy.dtensor_cfg.tensor_parallel_size}

  dtensor_cfg:
    _v2: true
    enabled: true
    cpu_offload: false
    sequence_parallel: false
    activation_checkpointing: true   # required at 8K+ sequence lengths
    tensor_parallel_size: 1
    context_parallel_size: 1
    custom_parallel_plan: null
    automodel_kwargs: {}
    lora_cfg:
      enabled: false
      target_modules: []
      exclude_modules: []
      match_all_linear: true
      dim: 8
      alpha: 32
      dropout: 0.0
      dropout_position: "post"
      lora_A_init: "xavier"
      use_triton: true

  megatron_cfg:
    enabled: false

  draft:
    enabled: false
    model_name: null
    loss_weight: 0.1
    num_layers: null
    aux_layer_indices: null

  dynamic_batching:
    enabled: false
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    sequence_length_round: 64

  sequence_packing:
    enabled: true
    train_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.train_micro_batch_size}}
    logprob_mb_tokens: ${mul:${policy.max_total_sequence_length}, ${policy.logprob_batch_size}}
    algorithm: "modified_first_fit_decreasing"
    sequence_length_round: 64

  optimizer:
    name: "torch.optim.AdamW"
    kwargs:
      lr: 3.0e-7                     # lower LR than math — coding tasks have longer credit horizon
      weight_decay: 0.01
      betas: [0.9, 0.999]
      eps: 1.0e-8
      foreach: false
      fused: false

  scheduler:
    - name: "torch.optim.lr_scheduler.LinearLR"
      kwargs:
        start_factor: 0.1
        end_factor: 1.0
        total_iters: 20
    - name: "torch.optim.lr_scheduler.ConstantLR"
      kwargs:
        factor: 1.0
        total_iters: 10000000000
    - milestones: [20]

  generation:
    backend: "sglang"
    max_new_tokens: ${policy.max_total_sequence_length}
    temperature: 1.0
    top_p: 1.0
    top_k: null
    stop_token_ids: null
    stop_strings: null
    sglang_cfg:
      async_engine: true             # required for async GRPO (dockyard SGLang async path)
      gpus_per_server: 1             # tensor-parallel GPUs per SGLang server; server count = inference GPUs / this
      dp_size: 1                     # data-parallel attention (ServerArgs) — leave 1 unless using DP attention
      pp_size: 1                     # pipeline-parallel size
      ep_size: 1                     # expert-parallel size
      dtype: ${policy.precision}     # bfloat16 — matches the trainer policy precision
      mem_fraction_static: 0.85      # SGLang static GPU memory fraction (≈ vLLM gpu_memory_utilization)
      context_length: ${policy.max_total_sequence_length}
      enable_memory_saver: true      # time-share GPU: release server memory during training, resume before generation
      skip_server_warmup: true       # avoid warmup timeout on cluster startup
    sglang_kwargs: {}
    colocated:
      enabled: true                  # SGLang is colocated-only (shares GPUs with the trainer)
      resources:
        gpus_per_node: 8
        num_nodes: 1

data:
  max_input_seq_length: ${policy.max_total_sequence_length}
  shuffle: true
  num_workers: 2
  use_multiple_dataloader: false     # async GRPO does not support multiple dataloaders

  train:
    dataset_name: "swe_bench"                       # DATASET_REGISTRY key → SWEBenchDataset
    hf_dataset_name: "princeton-nlp/SWE-bench_Lite"  # HF path passed to the loader
    split: "test"
    max_prompt_length: 4096
    verified_only: false
    shuffle_seed: ${grpo.seed}

  validation:
    dataset_name: "swe_bench"
    hf_dataset_name: "princeton-nlp/SWE-bench_Lite"
    split: "test"
    max_prompt_length: 4096
    verified_only: false
    subset_size: 64                  # further capped by grpo.max_val_samples

  default:
    processor: "swe_bench_data_processor"
    env_name: "code"

env:
  code:
    # Number of parallel scoring workers.
    num_workers: 16
    # "binary" → 1.0 when resolved else 0.0; "test_pass_rate" → pass fraction.
    reward_mode: "binary"
    # Zero the reward when the agent patch modifies a held-out test file.
    integrity_check: true
    # Per-task test wall-clock timeout (seconds), enforced executor-side.
    task_timeout: 300
    # Task-executor endpoints for /task/submit, round-robined across the batch.
    # Deployment-specific; can also be set via DOCKYARD_SANDBOX_URLS.
    sandbox_urls:
      - "http://localhost:9090"

logger:
  log_dir: "logs/grpo-swe"
  num_val_samples_to_print: 3
  wandb_enabled: false
  tensorboard_enabled: true
  mlflow_enabled: false
  swanlab_enabled: false
  monitor_gpus: true
  wandb:
    project: "dockyard-rl"
    name: "grpo-swe"
  tensorboard: {}
  gpu_monitoring:
    collection_interval: 10
    flush_interval: 10

cluster:
  gpus_per_node: 8
  num_nodes: 4