"""Generation backend identifiers.

Centralizes the backend name strings used in generation configs
(``generation.backend``) and in weight-sync transport selection.
"""

VLLM_BACKEND = "vllm"
SGLANG_BACKEND = "sglang"
