# Documentation build dependencies (dev/CI only — not part of the runtime image).
# Build the site with:  pip install -r docs/requirements.txt && (cd docs && make html)
#
# autodoc2 parses the source statically (AST), so the API reference builds
# without importing dockyard_rl — the heavy runtime deps (torch, vLLM, jax)
# need not be installed to render the docs.
sphinx>=7.2
myst-parser>=2.0
sphinx-autodoc2>=0.5.0
furo>=2024.8.6
sphinx-copybutton>=0.5.2
sphinxcontrib-mermaid>=0.9.2
