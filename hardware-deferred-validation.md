# Hardware-deferred validation ledger

**Validation of `dockyard_rl` is incomplete.** It is developed **without access to a GPU
or a multi-node cluster**, so a large part of the system has never been run on the
hardware it targets. Everything that can be checked statically or numerically on CPU
is — every change ships with `py_compile`, a clean canonical `pyright` pass, and CPU
unit/parity tests (model forward-logit parity against the HuggingFace reference,
loss/gradient parity against the torch reference, name-map round-trips, config
resolution, and byte-identity of the unchanged path when a feature is disabled). But the
checks that require real hardware **have not been performed**, because the resources to
run them are not available at the developer's end.

This ledger is the **honest record of that gap** — the validations that remain
**pending** until GPUs/a cluster are available: live NCCL/collective numerics, multi-GPU
sharding and device-mesh composition, CUDA-only kernels (`torch._grouped_mm`,
`ragged_all_to_all`), bf16 numerics, weight refit into a running inference engine, and
end-to-end training convergence. Each item below names the exact assumption that is
written to be correct but is **not yet verified**, why it cannot be tested here, and the
concrete check that would close it on hardware. Until that check is run, the item should
be treated as **unproven** — it may pass, or it may surface a real bug.

I publish this rather than imply full validation. If you have GPU/cluster resources and
run any of these pending checks, results (pass or fail) are very welcome — open an issue
or PR referencing the `HV-` id.

**How to read a row.** Each row records the area + source file, the unverified
assumption, a severity, and the bring-up check that closes it. Severity:
**High** = silent wrong training / crash if the assumption is wrong;
**Med** = degraded behavior or a loud failure that's easy to spot; **Low** = cosmetic /
perf only.

**Subsystems covered.** Torch MoE / expert-parallelism (HV-1–3, 8–15), weight-sync &
serving (HV-4, 7, 16), the async data plane (HV-17–18), rollouts & structured tool-use
(HV-19–23), preference/offline algorithms (HV-24–26), and the JAX trainer re-platform
(HV-27–33). The standing path to closing most of these is a single-node un-packed
bring-up on a tiny model, then a small MoE model for the EP-specific rows.

| ID | Area | Unverified assumption | Severity | Bring-up check |
|----|------|-----------------------|----------|----------------|
| HV-1 | B.2 mesh (`models/dtensor/moe/mesh.py`) | `ep_coord = global_rank % ep` — assumes `_unflatten` lays `ep` as the innermost/fastest axis of the sparse mesh `(dp_replicate, efsdp, ep)`. The rank→expert partition (and thus which expert weights land on which GPU) depends on this. | High | On a 2-GPU `ep=2` run, assert `experts_for_rank(rank, E)` matches the expert indices actually held in the sharded expert weight (`Shard(0)` on EP) on ranks 0 and 1. |
| HV-2 | B.2 mesh (`build_moe_meshes`) | Device-bound builder (`init_device_mesh` + `_unflatten` + `_flatten`) is py_compile-only; never run against a live process group. Signature matches torch 2.11 `_unflatten(dim, sizes, names)`. | Med | Build the meshes on a real ≥2-GPU cluster; confirm dense `(dp_replicate,dp_shard,cp,tp)` + flattened `dp` and sparse `(dp_replicate,efsdp,ep)` instantiate and group ranks as expected. |
| HV-3 | B.2 mesh (`build_moe_meshes`) | Degree-1 mesh dims still create trivial process groups; torchtitan elides these via a fake backend. Correctness holds without it; perf/PG-count only. | Low | At scale, add the fake-backend elision for degree-1 dims if PG creation overhead shows up. |
| HV-4 | A.1 #2513 (`vllm_worker_async.py::_install_engine_input_socket_lock`) | The private vLLM-0.22 attribute path `self.llm.engine_core.input_socket._shadow_sock.send_multipart` exists. Guarded: on `AttributeError` it logs a warning and SKIPS the lock (does not crash). If the path moved, the HTTP-server↔weight-update send serialization silently no-ops. | Med | With `expose_http_server=true`, confirm no warning is logged (i.e. the lock installed), and that concurrent generation + refit don't deadlock the engine. |
| HV-5 | A.1 #2176 (`grpo.py::add_grpo_token_loss_masks_and_generation_logprobs`) | Generated assistant messages always carry `generation_logprobs` (verified `include_logprobs=True` at all call sites). Fail-silent edge: if the backend returns no logprobs despite the request, generated tokens get `token_loss_mask=0` (train on nothing) instead of loudly failing. | High | On a real rollout, assert `generation_logprobs` is present on every generated assistant message before masking; spot-check that token_loss_mask sums > 0 per generated sequence. |
| HV-6 | A.1 #2380 (`distributed/virtual_cluster.py`) | `DOCKYARD_MASTER_PORT_RANGE_LOW/HIGH` are read on the REMOTE Ray node. If set only on the driver and not propagated via `runtime_env`, workers fall back to the constants (25000–28000). | Low | Confirm `ray.sub` exports the env to workers (or that the constants are acceptable); verify master port lands in the intended range. |
| HV-7 | A.2 weight_sync (`weight_sync/*`) | The 3 transports (Collective/IPC/HTTP) are validated only via mocked unit tests (`test_generation_refit.py`, 7/7). The real NCCL broadcast / CUDA-IPC ZMQ / SGLang HTTP weight transfer + the GPU phase transitions (offload/prepare_for_generation) are untested on hardware. | High | Single-node colocated (IPC) + non-colocated (NCCL) refit on real GPUs: confirm `sync_weights()` transfers weights correctly, `is_stale` toggles, and a post-refit generation reflects updated weights. |
| HV-9 | B.3 application (`models/dtensor/moe/parallelize_moe.py`) | `parallelize_grouped_experts` / `parallelize_router_gate` apply the B.3 placement policy via `distribute_tensor` over the B.2 sparse mesh. The placement DECISIONS are pure-tested (`test_moe_sharding.py`); the `distribute_tensor` application is device-bound, never run on a real mesh. | High | On a 2-GPU ep=2 run assert each expert weight becomes a DTensor with placements `[Replicate, Replicate, Shard(0)]` and `to_local()` shape `(num_experts/ep, *, *)`; router gate replicated. |
| HV-8 | B.4 grouped compute (`models/dtensor/moe/experts.py::GroupedExperts._experts_forward`) | SwiGLU expert FFN via `torch._grouped_mm` (private, CUDA-ONLY — confirmed it errors on CPU). The whole expert compute is unrunnable/untestable off-GPU. Present in dockyard's `torch==2.11.0+cu130`. | High | On GPU, run `GroupedExperts.forward` fwd+bwd; verify numerics vs a reference per-expert loop; guard the `_grouped_mm` call + a fallback if the private signature shifts. |
| HV-10 | B.4 EP dispatch (`models/dtensor/moe/dispatch.py::AllToAllTokenDispatcher`) | BUILT (Phase 4, 2026-06-09): cross-rank `AllToAllTokenDispatcher` (`all_to_all_single` for the per-expert count exchange + `all_to_all_single_autograd` for the token shuffle, then `_permute` rank-major→expert-major) distilled from torchtitan; `build_token_dispatcher("alltoall")` returns it; the v2 worker wires its EP submesh during `_parallelize` (`wire_ep_mesh(moe_mesh["ep"])`). The PURE parts are CPU-tested: `_permute`/`_unpermute` index math (hand-verified rank-major↔expert-major) + the `ep_mesh is None`→local fallback matching `LocalTokenDispatcher` byte-for-byte (`test_moe_alltoall_dispatch.py`). Unverified on hardware: the live `all_to_all_single` collectives over a real EP process group (NCCL), the count-exchange + split computation against actual cross-rank token distributions, and that the per-expert offsets feed the grouped-GEMM correctly. SP is unsupported on the MoE path (sp_size=1). | High | On a ≥2-GPU ep=2 cluster, confirm dispatch routes each rank's tokens to the owning experts' ranks, combine reverses it (round-trip identity end-to-end), and gradients flow through `all_to_all_single_autograd`. |
| HV-11 | B.5 v2 mesh (`dtensor_policy_worker_v2.py::_build_moe_device_mesh`) | Two unverified bits: (a) `dense[("dp","cp")]._flatten("dp_cp")` flattens a tuple that includes the already-flattened `dp` axis — whether torch's DeviceMesh allows flattening a composite containing a non-root (already-flattened) dim is not confirmed. (b) Unlike v1, this path does NOT set `LOCAL_RANK=0` around `init_device_mesh` (the flat world mesh is built inside `build_moe_meshes`), so on torch ≥2.8 device selection under single-visible-GPU may differ from v1. | High | On a ≥2-GPU HSDP/EP run, confirm the dense `dp_cp` flatten instantiates and that FSDP shards over the intended ranks; verify the device-mesh init binds the correct CUDA device (no LOCAL_RANK mismatch). |
| HV-12 | B.5 v2 parallelize (`dtensor_policy_worker_v2.py::_parallelize`, EP path) | **ep>1 only.** Routed experts are `distribute_tensor`'d over the SPARSE mesh, then `super()._parallelize` runs the dense FSDP/TP plan over the WHOLE model — the dense plan does NOT exclude expert params, and experts get no dedicated `fully_shard` over the `efsdp` axis. Whether FSDP re-wraps / conflicts on already-DTensor experts living on a different mesh is unresolved; torchtitan's flow excludes experts from the dense plan and FSDPs them separately over `efsdp`. NOTE: at **ep=1** (`enable_ep=False`) experts shard over the DENSE mesh like any other TP'd param, so dense FSDP handles them with no double-wrap — this item bites only at ep>1, which also requires HV-10 (all-to-all). **Phase-4 finding (2026-06-09):** this is NOT a small per-expert `fully_shard(efsdp)` add. torchtitan (`models/llama4/parallelize.py`) uses a UNIFIED `fully_shard(block, shard_placement_fn=...)` over a dedicated `edp_mesh` with a Shard(0)/Shard(1) choice (Shard(1) when `efsdp*ep > num_experts` to avoid padding), and does NOT pre-`distribute_tensor` experts over a 3D sparse mesh — it lets the FSDP `shard_placement_fn` assign expert-vs-dense params to different meshes/placements. The `parallelize_grouped_experts` path pre-distributes experts over the 3D sparse mesh (`Replicate` on efsdp, `Shard(0)` on ep), which diverges; reconciling the two (so the dense FSDP excludes experts and efsdp FSDP composes with the existing ep-Shard) is a device-bound EP+FSDP redesign that also touches the validated `_parallelize_model` + the mesh builder. Left for ep>1 bring-up (cannot validate FSDP/mesh composition on CPU). The dense seam (ep=1, dp_replicate=1) is byte-identical to v1. | High | On a 2-GPU ep=2 run, confirm experts stay `Shard(0)`-on-ep after `_parallelize` (not re-sharded by the dense FSDP); implement torchtitan-style unified `fully_shard(block, shard_placement_fn=...)` over an edp_mesh (reworking the pre-distribute_tensor approach) and verify experts FSDP over efsdp without double-wrap. |
| HV-13 | B.6 EP refit (`dtensor_policy_worker_v2.py::_iter_refit_state_dict` / `prepare_refit_info`, `moe/refit.py`) | Refit expands native fused experts → per-expert HF tensors: the v2 override `full_tensor()`-gathers each EP-sharded stack (device-bound, depends on HV-1 ordering) then unbinds dim 0. **Slice 3.3 (2026-06-09) CONFIRMED the name/shape convention against real vLLM 0.21 source**: `Qwen3MoeForCausalLM.get_expert_mapping` → `make_expert_params_mapping(ckpt_gate_proj_name="gate_proj", ckpt_down_proj_name="down_proj", ckpt_up_proj_name="up_proj")` yields `weight_name=f"experts.{i}.{proj}."`, `param_name="experts.w13_"\|"experts.w2_"`; the loader does `weight_name in name` then `name.replace(...)`. The emitted `...mlp.experts.{i}.gate_proj.weight` (up/down) substring-matches and resolves to `...experts.w13_weight`/`...experts.w2_weight`; unbound shapes `(F,D)`/`(D,F)` match the per-expert `nn.Linear.weight` the FusedMoE loader fuses. The dense `stacked_params_mapping` gate_up path correctly skips `mlp.experts`. Regression-locked CPU (`test_moe_refit.py`, 7/7, replays the vLLM mapping) + composes through the collective transport (`test_generation_refit.py`). RESIDUAL (device-bound): (a) the live `full_tensor()` gather + actual load into a running vLLM engine + a post-refit generation reflecting updated experts; (b) confirm the vLLM **0.22** pin keeps the same `make_expert_params_mapping` ckpt-name API (verified on the installed 0.21; longstanding/stable, but the image pins 0.22). Only fires when native GroupedExperts exist (now produced by the B.5a surgery, slices 3.1+3.2). | High | On a 2-GPU ep=2 MoE refit, assert the gathered+unbound per-expert tensors load into vLLM 0.22 and a post-refit generation reflects updated experts. |
| HV-14 | B.5b load balance (`dtensor_policy_worker_v2.py::_reduce_tokens_per_expert`, `moe/load_balance.py`) | The aux-loss-free expert-bias updater (optimizer step pre-hook) needs a GLOBAL per-expert token load before stepping `expert_bias_E`. The v2 reduce sums `tokens_per_expert_E` over the `dp_cp` group (data+context complement — ranks that saw different token shards). Two unverified bits: (a) whether `dp_cp` is the correct/complete reduction set, or the EP axis and/or the tp-replicated counts must also be folded in/excluded (tp ranks hold identical counts; reducing over tp would over-count); (b) that an all-reduce'd identical load yields an identical `expert_bias_E` step on every rank (the bias buffer is replicated, not a DTensor). The pure sign-step math + counter reset are CPU-tested (`test_moe_load_balance.py`, 10/10) with `reduce_tokens_fn` injected. Only fires when load-balanced native `MoEBlock`s exist (gated on B.5 surgery + `load_balance_coeff` set). | Med | On a 2-GPU ep=2 run with `load_balance_coeff>0`, confirm the reduced per-expert load matches the global token routing, every rank's `expert_bias_E` stays identical after the step, and balance improves over training. |
| HV-15 | B.5a surgery (`models/dtensor/moe/surgery.py` + worker call-site, target Qwen3-30B-A3B) | Surgery swaps each `Qwen3MoeSparseMoeBlock` for a native `MoEBlock`, restructuring the transformers-5.x FUSED experts (`gate_up_proj (E,2F,D)` split at F → `w1/w3_EFD`; `down_proj`→`w2_EDF`) + copying `gate.weight`→router. **Slice 3.2 wired the worker call-site** (`_apply_moe_surgery` seam, run on the rank-0 source model before `state_dict()` capture AND the empty per-rank model inside `init_empty_weights`, before `_parallelize`). The module-tree shape, fused param names, gate/up split VALUES, and the attention-only TP-plan registration are CPU-verified (`test_moe_surgery.py`, 13/13). Unverified on hardware: (a) the live grouped-GEMM forward of the surgically-built experts (CUDA-only, ties to HV-8) matches HF's per-expert reference on the 30B checkpoint; (b) the real 30B `from_pretrained` load + the per-layer `.clone()` peak memory (rank 0) are acceptable; (c) the exact transformers point-version keeps the `gate_up_proj`/`down_proj`/`gate.weight` attribute names (stable across 5.x, pinned by vLLM 0.22); (d) **`set_model_state_dict` broadcast key-match** — rank-0 (real) and empty (meta) models go through identical surgery so native keys match, and the worker's `named_buffers()` broadcast covers the non-persistent `tokens_per_expert_E` (relies on `init_empty_weights` keeping buffers on CPU — empirically true in this env's accelerate; a future accelerate defaulting buffers→meta would break this for the whole meta-init flow, not just MoE). Only the ep=1 (local dispatcher) path is constructible until HV-10. | High | On GPU, load Qwen3-30B-A3B, run surgery, assert a surgically-MoE layer's forward matches the original HF `Qwen3MoeSparseMoeBlock` (logits within tolerance) and that the rank-0 state dict broadcasts cleanly into the meta model; confirm peak memory + attribute names + the accelerate buffer-placement against the image. |
| HV-16 | Phase 3b FP8 serving (`models/generation/vllm/quantization/fp8.py`) | FP8 inference serving is PRE-BUILT + wired (init_fp8 engine kwargs, block-wise 128x128 weight cast, quantize-on-refit `load_weights`, `process_weights_after_loading_moe`); training stays bf16. The MoE refit COMPOSITION is CPU-verified (slice 3b.1): native-experts→per-expert refit (3.1-3.3) feeds `fp8.load_weights`, which block-casts each per-expert `(F,D)`/`(D,F)` tensor and emits `k+"_scale_inv"`; vLLM's expert mapping routes the weight + `_scale_inv` (block-quant `weight_scale_name="weight_scale_inv"`) to `experts.w13_/w2_weight[_scale_inv]`; the FP8-MoE vLLM symbols exist in the installed vLLM. Unverified on hardware: (a) the live block-FP8 grouped-GEMM forward + numerical parity vs bf16 on Qwen3-30B-A3B (Hopper/Blackwell); (b) the FusedMoE FP8 weight_loader actually accepts the per-expert `_scale_inv` block-scales via the gate/up/down shard_ids and the resulting w13/w2 scale layout is correct; (c) the per-refit re-quantization (`process_weights_after_loading_moe` `.copy_()`-preserving the weight_loader) holds across repeated refits; (d) the vLLM **0.22** pin keeps the `Fp8MoEMethod`/`Fp8LinearMethod`/`BaseKVCacheMethod` internals the patches monkeypatch (installed proxy 0.21). The import/patch-time deps were AUDITED against installed vLLM (slice 3b.2): all present, and one real path bug was FIXED (`RayDistributedExecutor` was imported from the non-existent `ray_distributed_executor` instead of `ray_executor` — latent crash for `model_parallel_size>1` FP8; now a version-robust dual import). Tests: `test_fp8_moe_refit.py` (5, cast shape + name routing, CPU). | High | On a Hopper/Blackwell FP8 run of Qwen3-30B-A3B (incl. TP>1), confirm a post-refit FP8 generation matches the bf16 reference within tolerance, the per-expert `_scale_inv` scales load into the FusedMoE, the ray-executor patch path fires for TP>1, and repeated refits stay stable. |
| HV-17 | Phase 5 #2672 worker-side data plane (`data_plane/worker_mixin.py::TQWorkerMixin`, `models/policy/tq_policy.py::TQPolicy`) | Meta-driven dispatch: `TQPolicy` bootstraps the controller, forwards `setup_data_plane` to workers, and dispatches `train_presharded`/`get_logprobs_presharded`/`get_reference_policy_logprobs_presharded` over `run_all_workers_sharded_data(in_sharded_axes=["data_parallel"], replicate_on_axes=[cp,tp,pp])` after `shard_meta_for_dp`; workers `_fetch` their slice from the plane (leader-fetch + NCCL broadcast when `_get_replica_group()` returns a (cp,tp) submesh, else independent fetch) and write per-token logprob deltas back via the leader-rank `_write_back_result_field`. Leader gate = `NamedSharding.is_axis_zero(_local_coords(), REPLICATED_AXES)` fed by the DTensor worker's `device_mesh["tp"/"cp"].get_local_rank()`. CPU-validated: `KVBatchMeta` pure transforms + leader-gate math (`test_data_plane_meta.py`, 11; named_sharding `is_axis_zero` sanity). Unverified on hardware: (a) the live `_broadcast_batched_data_dict` two-phase NCCL broadcast (descriptor via `broadcast_object_list` + per-tensor `broadcast`, CPU→CUDA move) on a real replica group; (b) `setup_data_plane` attaches every worker as a `bootstrap=False` client to the driver-created controller; (c) leader-only write-back is exactly-once (no duplicate-write controller error) under tp/cp>1; (d) `shard_meta_for_dp` balanced per-rank sharding matches the worker fetch. Needs `tensordict` + `transfer_queue` (image-baked) + a cluster. | High | On a ≥2-GPU run with `data_plane.enabled` + tp or cp >1, confirm only the (cp,tp) leader writes back, non-leaders receive the broadcast, prev/ref logprobs land on the plane, and `train_presharded` fetches the union; verify no duplicate-write errors. |
| HV-18 | Phase 5 #2672 async data-plane dispatch (`algorithms/async_utils/trajectory_collector.py`, `algorithms/grpo.py::_dataplane_train_step` + `async_grpo_train` consume branch) | Async-native dispatch (no upstream reference — upstream's is sync-only). Collector writes each per-prompt group's bulk to the plane (`_flatten_group_for_dataplane` → `kv_first_write`, sample_ids `{uid}_g{i}`) and enqueues `{meta, driver_carry}` (no bulk in the ReplayBuffer); trainer concats per-group metas (`KVBatchMeta.concat`) + driver_carry, runs `_dataplane_train_step` (get_logprobs_from_meta/get_reference_policy_logprobs_from_meta → narrow `read_from_dataplane` of prev/gen logprobs+token_mask+sample_mask → seq-logprob-error masking → `compute_advantage` on carry-derived adv_inputs → `write_to_dataplane` advantages+post-mask sample_mask → `train_from_meta`), then per-step `finish_step` clears consumed ids. ONE persistent partition at startup (`prepare_step(num_samples=buffer*group, group_size=None)`); ReplayBuffer stays the sampling/version coordinator (TQ balanced sampling bypassed). All gated on `master_config.data_plane` (None → byte-identical legacy path; 133 unit tests green confirm the disabled path is unregressed). DP-mode per-step jsonl omits the token-id bulk (input_ids off-driver); metrics sourced from a returned `dp_payload`. `force_on_policy_ratio=True` explicitly unsupported here (raises). Correct-by-construction (mirrors grpo_sync's data-flow + the existing async consume body) but ENTIRELY runtime-untestable here: no `tensordict`, no `transfer_queue`, no cluster, and importing collector/grpo pulls the worker/nccl chain → py_compile + pyright only. | High | On a multi-node `data_plane.enabled` async run, confirm: collector writes bulk to the plane and the buffer holds only metas; the persistent partition row count stays bounded (finish_step clears per step); advantages/sample_mask deltas land under the same sample_ids; `train_from_meta` fetches the union and trains; convergence matches the colocated path on a small model. |
| HV-19 | Phase 6 #2656 rollout-loop verdict collection (`experience/rollouts.py` per-turn detection in `run_multi_turn_rollout` / `run_sample_multi_turn_rollout`, env stamping in `multi_turn_session_env.py` / `code_environment.py`) | The invalid-action penalty's pure layers are fully CPU-tested (`test_invalid_action_penalty.py`, 29: detectors, tier composition, env-flag pop hygiene, `apply_invalid_action_penalty` arithmetic incl. floor/unshaped/disabled-no-op). Unverified at runtime: the live per-turn wiring — that the just-appended assistant message is the one assessed (`message_log[global_idx][-1]` / `current_message_log[-1]` at env-step time), that env-stamped `invalid_action` flags arrive through a real `env.step` + `calculate_rewards` round-trip and are popped before metadata persists into `extra_env_info`, and that the count fields survive group slicing into the ReplayBuffer / `driver_carry` (DP). Needs a generation engine, like all rollout-loop changes. Disabled config is byte-identical (no detection runs; 173/173 unit tests green). | Med | On a small live run with `invalid_action_penalty.enabled` + a nonzero penalty, emit a deliberately command-less turn and confirm `invalid_action_rate>0`, the count lands in the batch/driver_carry, the reward drops by exactly the configured penalty, and `extra_env_info` carries no verdict keys between turns. |
| HV-20 | GDPval agentic file-producing env (`environments/gdpval_agentic_environment.py`, `environments/_gdpval_extract.py`, `ubuntu-base/ubuntu-swe-gdpval.dockerfile`) | The pure layers are fully CPU-tested (`test_gdpval_agentic.py`, 32: extractor manifest/content/limit/fallback, command builders, deliverable composition, `_start_spec`/`_after_start`/`_finish_and_score` with the sandbox client + reward stubbed, dataset agentic format_data + text-path byte-identity + reference encoding, processor routing, registry wiring). Unverified at runtime / needs the image: that the agent actually produces files in a live `ubuntu-swe-gdpval` container; that the shipped extractor reads real xlsx/docx/pptx/pdf via the image's openpyxl/python-docx/python-pptx/pypdf (the host can only test the text/fallback paths — binary extractors degrade gracefully when libs are absent); that `session_export`/`session_exec` round-trips at finish; and the `ubuntu-swe-gdpval` docker build (the in-Dockerfile import check gates dependency breakage). Reference-file *fetching* from HF binary URIs is a separate documented follow-on (mechanism built/tested, default off). | Med | Build `ubuntu-swe-gdpval`, run a small live GDPval-agentic episode: confirm the agent writes files under `/workspace/deliverable`, the extractor returns a manifest + real xlsx/docx/pptx text, the rubric judge scores file/format criteria the text path cannot, and `_after_start` stages provisioned reference files. |
| HV-21 | Structured tool-use turn-envelope + decode fidelity (`experience/rollouts.py`) | The localized turn-envelope construction (fork 3) wraps each tool result in `<\|im_start\|>user\n<tool_response>\n{content}\n</tool_response><\|im_end\|>\n<\|im_start\|>assistant\n` before tokenizing, as a single role-masked message, and decodes assistant turns with `skip_special_tokens=False` when configured so `<tool_call>` tags survive. The token-id alignment invariant + off-path parity are CPU-tested with a fake tokenizer (`test_structured_rollout.py`, 11; 4 grpo-resolver cases skip without the nccl stack). Unverified on hardware / against the real tokenizer: (a) the **exact envelope bytes** match the real Qwen3.x chat template (the marker constants are the documented default, PINNED at the build-time gate, not from memory); (b) the decode now AUTO-DETECTS whether the Qwen3.x tokenizer marks `<tool_call>`/`</tool_call>` special (round-trip probe in `_structured_decode_skip_special`, memoized) and keeps special tokens when they would be stripped — so the prior silent-failure footgun is removed; the gate only needs to confirm the auto-detection picked the expected value (explicit `decode_skip_special_tokens` still overrides); (c) the live multi-turn rollout produces a chat structure the model re-enters as the assistant role and emits well-formed calls. Needs a generation engine. Disabled (`structured_tool_use` unset) → byte-identical fenced path. | High | On a small live Qwen3 run with `grpo.structured_tool_use.enabled` + an env `structured_tools: true`, confirm the envelope bytes equal the tokenizer's template render, `<tool_call>` survives decode, the model emits parseable calls across ≥2 turns, and the loss mask covers only assistant tokens. |
| HV-22 | Structured tool-use constrained decoding (`models/generation/vllm/{utils,vllm_worker,vllm_worker_async}.py`, `interfaces.py`) | Per-sample `StructuredOutputsParams(structural_tag=…)` plumbing (fork 2). `build_structural_tag` emits the vLLM 0.21 legacy `structures`/`triggers` shape (verified against `v1/structured_output/backend_xgrammar.py`: `StructuralTagItem(begin/schema/end)` + triggers), and `_build_sampling_params` is byte-identical when no spec is supplied (CPU-tested, `test_structured_decoding.py`, 18; xgrammar-compile case skips when xgrammar absent). Unverified on hardware / needs xgrammar in the image: (a) the structural_tag actually compiles + constrains only the `<tool_call>` scaffolding on a live engine (leaving thinking/arg-values free); (b) **the in-JSON forced-skeleton tokens are NOT recoverable** — `derive_structural_submask` only marks the CPU-derivable `<tool_call>`/`</tool_call>` delimiter spans; the forced `{"name":...,"arguments":{` skeleton inside requires the per-step matcher bitmask, which vLLM 0.21 does not surface on `CompletionOutput` (an engine-side patch is needed for full fork-2 loss-masking of forced tokens); (c) the legacy structural-tag shape may need updating on a vLLM upgrade (0.21 labels it deprecated). Off by default (`constrained_tools` empty / `structured_tool_use` unset) → byte-identical sampling params. | High | With xgrammar in the image, on a live run with `constrained_tools: [submit_patch]`, confirm the structural tag compiles, malformed wrappers become impossible while argument values stay free, and (after the engine patch) the forced-skeleton submask aligns to output_ids for loss-masking. |
| HV-23 | Structured tool-use env dispatch + prompt advertisement (`environments/{multi_turn_session_env,code_environment}.py`, `data/processors.py`, `data/utils.py`) | Env parsers dispatch `assess_tool_calls` (run_shell/read_file/write_file/task_complete; submit_patch) and stamp `invalid_action`; processors advertise the registry via `apply_chat_template(tools=…)`; `setup_response_data` threads `grpo.structured_tool_use` onto each `TaskDataSpec`. All CPU-tested (`test_structured_env_parsing.py` + `test_structured_prompt_construction.py`, 69) incl. flag-off byte-identity. Unverified at runtime: (a) `read_file`/`write_file` shell commands (`cat --`, base64 write) round-trip through a live executor session; (b) the prompt the real Qwen3 tokenizer renders with `tools=` matches expectations and the model uses the advertised tools; (c) the end-to-end gate (config → TaskDataSpec → processor → prompt; env flag → parser) activates together. Needs the sandbox executor + a generation engine. Disabled → byte-identical. | Med | On a small live run with the protocol enabled, confirm the prompt advertises the env's tools, the agent calls them, `read_file`/`write_file` execute in-container, and an invalid call is stamped + penalized (with HV-19). |
| HV-24 | KTO end-to-end (`algorithms/loss/loss_functions.py::KTOLossFn`, driver `algorithms/dpo.py`) | The KTO unpaired value function (`r=Σ(logπ−logπ_ref)`; `desirable λ_D(1−σ(β(r−z)))`, `undesirable λ_U(1−σ(β(z−r)))`, TRL-faithful) + `kto_collate_fn` (unpaired, emits `preference_label`) + `sequences_per_datum` contract are fully CPU-tested (`test_dpo_loss_variants.py`). DEFERRED (two pieces, do together): (a) the worker-side **KL reference z** — mismatched/shuffled-completion logprobs through the CURRENT policy each step (detached, gathered across DP, clamped ≥0, per TRL); not precomputable offline (the policy changes each step) so it needs a worker forward pass; the loss consumes `data["kto_reference_kl"]` and defaults z=0 when absent (pre-worker). (b) **driver variant-awareness** — `dpo.py` assumes paired data (`gbs*2`/`mbs*2`, `add_ref_logprobs_to_data` doubling); KTO unpaired needs the driver to read `loss_fn.sequences_per_datum` (=1 for KTO; default 2 keeps the DPO family byte-identical) and to emit `preference_label` + `kto_reference_kl` per microbatch. Until both land, KTO is unit-validated but not runnable end-to-end. | High | On a GPU DPO run with `dpo.loss_variant=kto` + an unpaired KTO dataset, confirm z is computed from mismatched completions, the unpaired batch trains (no `[::2]` split), desirable/undesirable terms move the model the right way, and DPO-family runs stay byte-identical (`sequences_per_datum=2`). |
| HV-25 | Online/iterative DPO live wiring (`algorithms/online_dpo.py`) | The online-preference core is fully CPU-tested with injected stubs (`test_online_dpo.py`, 29): `PreferenceJudge` (pointwise rubric score via `HLEJudgeClient.chat` + verifiable-`RewardFunction` fallback), best-vs-worst pairing with the score-margin gate, `pairs_to_preference_data` → the existing `preference_collate_fn`, `JudgeOveroptMonitor` (judge-drift + KL-target flag), and `run_online_dpo_iteration`/`online_dpo_train`. DEFERRED: binding `generate_fn` (K-repeated prompts → `policy_generation.generate` → decode to `Candidate`s) and `train_fn` (`add_ref_logprobs_to_data` + `policy.train(loss_fn)` + `WeightSynchronizer.sync`) to the real inference/trainer fleets; live generation + judge throughput. `regeneration_period` buffered (off-policy) reuse currently regenerates every step. The frozen-ref KL anchor rides the D0–D4 loss factory unchanged. | Med | On a GPU run, wire `generate_fn`/`train_fn`, confirm K candidates/prompt are generated + judge-scored, margin-gated best-vs-worst pairs train via the existing DPO step, the frozen-ref KL anchor holds, and `JudgeOveroptMonitor` flags KL drift. |
| HV-26 | DTensor pretrained-checkpoint init (`utils/checkpoint.py::get_pretrained_paths`; setup in sft/rm/dpo/distillation/grpo) | Path resolution is CPU-tested (`test_pretrained_checkpoint.py`, 6): `get_pretrained_paths`/`get_resume_paths` locate `policy/weights`[`/optimizer`] under a checkpoint dir and honor `restore_optimizer`; the 5 setups source `(weights,optimizer)` from a prior dockyard checkpoint dir when there is no resume, keeping training state fresh (step 0). The actual tensor load is the SAME DTensor loader resume already uses (`Policy(weights_path, optimizer_path)`) — no new backend (this replaced the excised Megatron `pretrained_checkpoint`). DEFERRED: the live load of a prior run's weights(+optimizer) into the DTensor worker and a forward/train confirming an SFT→DPO→GRPO hand-off initializes correctly and starts at step 0. | Med | On a GPU run, point `pretrained_checkpoint.path` at a prior dockyard checkpoint dir; confirm the new run loads its weights (+optimizer when `restore_optimizer`), starts at step 0 (fresh training state), and that resume (`last_checkpoint_path`) still takes precedence. |
| HV-27 | JAX trainer refit seam → live vLLM (`models/jax/refit.py`, J5) | The JAX→HF name-map + transpose inverse + `(shape,dtype)` info + jax→torch value round-trip are CPU-tested (`test_jax_refit_roundtrip.py`, 3: HF→J1-load→J5-emit reproduces the HF state dict exactly; `prepare_refit_info` HF-layout shapes; dlpack/numpy value round-trip incl. transposed kernels). The MoE (`test_jax_moe_refit.py`) and **Qwen3-Next** (`test_jax_qwen3_next_refit.py`, 3) name-maps are CPU round-trip-tested too — the qwen3_next inverse covers its specials (conv `(C,K)→(C,1,K)`, `dt_bias`/`A_log` passthrough, shared-expert rename, gated/`(1+weight)` norms) and re-fuses experts to the FusedMoE per-expert layout; the worker routes qwen3_next refit. DEFERRED (GPU-only): (a) jax→torch **dlpack zero-copy on CUDA** (`torch.from_dlpack(jax_array)`, no host bounce) — CPU test uses the numpy fallback; (b) feeding the `(hf_name, torch.Tensor)` stream through the unchanged `utils/packed_tensor.py::packed_broadcast_producer` over the torch NCCL `model_update_group` into live vLLM; (c) the bf16 refit dtype-cast in the producer `post_iter_func` (J9 wiring) matching `prepare_refit_info(dtype="bfloat16")`; (d) two co-resident collective worlds (XLA mesh + torch NCCL) sharing the GPU under `XLA_PYTHON_CLIENT_PREALLOCATE=false`. MoE fused-expert re-expansion on refit = J8. | High | On a ≥2-GPU run with the JAX trainer selected, confirm a refit broadcasts JAX params (dlpack, no host copy) into vLLM, generations reflect the updated weights, and XLA/NCCL co-residency leaves headroom (no OOM). |
| HV-28 | JAX trainer Ray-actor bring-up (`models/jax/policy_worker.py` config path, `models/policy/lm_policy.py` jax_cfg branch, J9) | CPU-tested (`test_jax_worker_interface.py`, 3): the full PolicyInterface/Colocatable surface (train with a real ClippedPGLossFn → loss-cfg derived from the loss_fn matches the stored cfg; get_logprobs/reference/topk; prepare_refit_info; save_checkpoint; lifecycle no-ops; shutdown), and the deferred methods raise clearly. `lm_policy` jax_cfg gate flip is **byte-identity-safe** (full unit suite 669 passed/54 skipped with JAX disabled). DEFERRED (GPU/cluster): (a) the config-path `_build_model_and_reference` live download + `load_hf_state_dict` of a real model_name (Qwen3 dense only at J9); (b) **one-actor-per-device `jax.distributed.initialize`** coordinator wiring (address/process_id/num_processes from the Ray placement) — `_init_jax_distributed` is a no-op stub; (c) the `RayWorkerBuilder.options().remote(config, **worker_kwargs)` actor construction + worker_group dispatch; (d) the data-plane `*_from_meta` variants (raise NotImplementedError — torch-transport integration deferred). | High | On a ≥2-GPU run with `policy.jax_cfg.enabled=true` + a Qwen3 model, confirm the worker_group builds, each actor joins the jax mesh via jax.distributed, a sync GRPO step (train + get_logprobs) runs, and a refit (HV-27) lands in vLLM. |
| HV-29 | JAX MoE EP all-to-all live numerics (`models/jax/moe/alltoall.py`, J8c) | CPU-tested (`test_jax_moe_alltoall.py`, 5): the rank-major↔expert-major permutation index math (`expert_major_permutation` vs a hand-computed reference + as an invertible permutation), `local_expert_counts`, `wire_ep` divisibility validation, and the **EP-disabled local fallback** byte-identical to `LocalTokenDispatcher` (also end-to-end: a Qwen3-MoE built with `token_dispatcher="alltoall"` but unwired == the `"local"` model, forward-identical). DEFERRED (multi-device only): (a) the two `jax.lax.ragged_all_to_all` collectives in `dispatch`/`combine` (count-exchange via `jax.lax.all_to_all` + the ragged token shuffle) run inside a `shard_map` over the `ep` axis — no semantics without a real multi-device `ep` mesh; (b) `wire_ep(ep_axis, ep_size)` called at parallelize time from the worker after the mesh is built; (c) numerical parity of the full ep>1 MoE forward (all-to-all → grouped GEMM → reverse all-to-all → scatter-add) vs the ep=1 result. **NOT CPU-de-riskable (probed 2026-06-13):** `jax.lax.ragged_all_to_all` raises `UNIMPLEMENTED: HLO opcode ragged-all-to-all is not supported by XLA:CPU ThunkEmitter` even on a multi-logical-device CPU mesh — unlike `ragged_dot`/`all_to_all`/`psum`, this primitive has no CPU lowering, so its numerics are genuinely GPU-only. Do not attempt the CPU multi-device pull-forward for this collective. | High | On a ≥2-device run, wire `ep` on the mesh, build a Qwen3-MoE with `token_dispatcher=alltoall`, and confirm the ep>1 forward matches the ep=1 forward (tokens routed cross-device and back), then matches HF. |
| HV-30 | JAX MoE EP weight sharding + refit gather (`models/jax/sharding.py` expert axes, `models/jax/moe/refit.py` `materialize`, J8b/J8d) | CPU-tested: expert `logical_axes` placement on a 4-device host mesh (`test_jax_moe_sharding.py`: expert dim over `ep`, hidden over `tp`); the refit re-expansion name/shape/unbind is pure and round-trips (`test_jax_moe_refit.py`). DEFERRED (multi-GPU): (a) experts actually `Shard(0)`-over-`ep` on a real GPU mesh and the grouped GEMM running on the local expert shard; (b) the refit `materialize` = EP gather (the `full_tensor`-equivalent over the `ep` axis) reconstructing experts in global index order before the per-expert unbind; (c) the gathered per-expert stream loading into a live vLLM `FusedMoE`. | High | On a ≥2-GPU `ep>1` run, confirm experts shard over `ep`, a forward uses only local experts, and a refit gathers + emits per-expert tensors that load into vLLM (generations reflect updated MoE weights). |
| HV-31 | JAX MoE load-balance cross-rank token reduce + jitted buffer accumulation (`models/jax/policy_worker.py::_make_lb_update_fn`, `models/jax/train_step.py` `pre_optim_step_fn`, J8 integration) | CPU-tested (`test_jax_moe_integration.py`, 4): the aux-loss-free bias step is wired as the train-step pre-optim hook — a real `worker.train` accumulates per-expert load through `nnx.value_and_grad` (buffer mutation **does** propagate back eagerly), steps `expert_bias_E` zero-centeredly on the `coeff` grid, and zeroes `tokens_per_expert_E`; `eval_mode` does not step; dense models get no updater; `_wire_ep_dispatchers` installs the `ep` axis on all-to-all dispatchers only. The JAX analogue of HV-14. DEFERRED (multi-device): (a) `reduce_tokens_fn` is hardcoded `None` (single-process exact) — under DP/CP the per-expert load must be summed over the data complement (an `all_reduce` / `jax.lax.psum` over the dp/cp mesh axes) or each replica's `expert_bias_E` diverges and routing desyncs across replicas; (b) ~~buffer accumulation under a jitted step~~ **RESOLVED on CPU (2026-06-13, `test_jax_moe_integration.py::test_load_balance_survives_nnx_jit`):** under `nnx.jit` the forward propagates the `tokens_per_expert_E` Buffer mutation (0 → counts) AND `update_expert_biases` runs *inside* `nnx.jit` (bias steps, counter zeroes) — nnx threads the buffer state, so J10's jitted train step needs **no** functionalization of the `pre_optim_step_fn`. Jit semantics are backend-independent, so this carries to GPU. Only (a) the cross-replica reduce remains deferred. | Med | On a ≥2-device DP run with `moe_load_balance_coeff>0`, confirm the reduced per-expert load is the global routing count, every replica's `expert_bias_E` stays identical after the step, balance improves over training, and the jitted train step carries the counter correctly. |
| HV-32 | JAX trainer bf16 numerics (whole `models/jax/*` forward + loss + grad + optax step) | All JAX↔HF parity is **fp32 on CPU** (J1/J3/J8/J11 tests at atol 2e-4…2e-3). The trainer is designed to run **bf16** on GPU, but no test confirms the bf16 forward/loss/grad/update stays within tolerance of the fp32/HF reference (accumulation order, RMSNorm `(x*w).to(bf16)` ordering, `ragged_dot`/attention in bf16, optax `bf16` master-weight choice). Not a standalone row until now — only obliquely noted in HV-27(c) (the refit cast) and the J3 audit. **Note: Turing (T4, sm_75) has no native bf16 → not representatively testable on T4; needs Ampere+ (A100/A10/L4).** | Med | On an Ampere+ GPU, run a small Qwen3 dense + Qwen3-MoE + Qwen3-Next forward/train in bf16; confirm logits/loss/grad match the fp32 (and HF) reference within a bf16-appropriate tolerance, and that a few optimizer steps reduce loss without divergence. |
| HV-33 | Online-DPO / RLAIF / eval launcher fleet seams (`examples/run_online_dpo.py`, `run_rlaif.py`, `run_eval.py`) | The injected-callable cores are CPU-unit-tested in their modules (online_dpo: judge, best-vs-worst pair construction, loop; rlaif: critique→revise + pair constructors; dpo_evals: ECE/Brier/pass-rate). The **launchers** are structurally validated only (py_compile, config load+resolve, canonical pyright clean). DEFERRED (live GPU fleets + judge endpoint): (a) `run_online_dpo._make_generate_fn` — build `GenerationDatumSpec` (padded `input_ids`/`input_lengths`), `policy_generation.generate(greedy=False)` for K samples/prompt, slice `output_ids[in_len:total]` → decode → `Candidate` (mirrors `experience/rollouts.py:210-247`); (b) `_make_train_fn` / rlaif `_make_train_fn` — `get_reference_policy_logprobs` → `torch.roll(-1)` into `reference_policy_logprobs` → `policy.train(batch, dpo_loss_fn, gbs=batch.size, mbs)` → `weight_synchronizer.sync_weights` (mirrors `dpo.py:295-300`); (c) `_extract_prompt_token_ids` pulling prompt token_ids from the live dataloader's message-log batch; (d) the LLM judge transport (`HLEJudgeClient`) reachable for `PreferenceJudge.score` / rlaif critique-revise; (e) `run_eval` reuses `grpo.setup` + `grpo.validate` — needs the refit-into-vLLM (HV-27 analogue) + a live env (math verifier or sandbox) to score. | Med | On a single-GPU colocated run with a small model: (run_eval) sync weights + `validate` returns reward metrics on the math env; (online_dpo) one iteration generates K candidates, the judge scores, a margin-gated pair trains a DPO step, weights refit; (rlaif) one prompt → response → constitutional revision → DPO step. Then confirm `dpo_evals` calibration/safety gates compute on a held-out set. |
| HV-34 | Sandbox NetworkPolicy enforcement (`infra/dockyard_k8s` `build_sandbox_network_policy`, `examples/sandbox-pool.yaml`) | The NetworkPolicy is rendered/validated structurally only (unit tests on the generated dict; clean schema round-trip). NOT verified that a real CNI **enforces** it: that ingress to port 9090 from outside the namespace / from non-matching pods is actually dropped while the in-namespace GPU fleets still reach the executor over the Service, and that egress (open by default, or DNS+CIDR when locked down) lets repo cloning succeed. A non-enforcing CNI makes the object an inert no-op — unverifiable without a NetworkPolicy-capable cluster. | High | On a cluster with a NetworkPolicy-enforcing CNI (Cilium/Calico): confirm a pod in another namespace cannot reach `dockyard-sandbox:9090`, the trainer pods can, and a task submit that clones a repo still succeeds; then with `allowEgress:false`+CIDRs, confirm DNS + allowed hosts work and others are blocked. |
| HV-35 | PodDisruptionBudget under voluntary disruption (sandbox `maxUnavailable:1`; GPU gang `kuberay.pdb` bringup) | The PDB objects render correctly (selector by `app.kubernetes.io/name` for the sandbox, `ray.io/cluster=<name>` for the gang; exactly one of min/max enforced by schema). NOT verified that the API server's eviction path honours them during a real `kubectl drain` / node upgrade — that the sandbox keeps ≥N-1 replicas and a too-strict GPU `minAvailable` does not silently wedge maintenance. The `ray.io/cluster` selector match also depends on KubeRay's actual pod labels at runtime. | Med | Cordon+drain a node hosting sandbox replicas and confirm the eviction respects `maxUnavailable:1` (rolling, not all-at-once); enable `kuberay.pdb` on a bringup cluster, confirm the `ray.io/cluster` selector matches real KubeRay pods, and that a drain blocks below `minAvailable`. |
| HV-36 | securityContext baseline does not break the workloads (`infra/dockyard_k8s` `build_security_contexts`; sandbox default-on, GPU opt-in) | CPU-validated: the contexts emit correctly (drop ALL caps, `allowPrivilegeEscalation:false`, seccomp RuntimeDefault; `runAsNonRoot`/`readOnlyRootFilesystem` opt-in). NOT verified at runtime that the **sandbox executor** still clones repos + runs test subprocesses under dropped capabilities + RuntimeDefault seccomp, nor — when `infra.security.enabled` is turned on for the GPU fleets — that the **CUDA/NCCL** stack (device access, IB/RoCE, shared-memory transport) survives dropped capabilities / non-root. GPU hardening stays opt-in precisely because this is unproven. | Med | Run the sandbox pool with the baseline and confirm a real `POST /task/submit` (clone → patch → pytest) passes; separately, enable `infra.security.enabled` on a GPU run and confirm training + NCCL weight-sync still work (or record which capabilities/runAs settings must be relaxed). |
| HV-37 | GGUF export + serve guard (`utils/gguf_export.py`, `models/generation/vllm/quantization/gguf_hardening.py`) | Export (`convert_hf_to_gguf`/`export_gguf_variants`) and validation (`validate_gguf` via the `gguf` package) are CPU-tested with mocked subprocess + a real gguf write/read round-trip (`test_gguf_export.py`, 6); the #51 dequant dim-guard is unit-tested against the real vLLM op symbol (`test_gguf_hardening.py`). Unverified / needs tooling + GPU: (a) live llama.cpp `convert_hf_to_gguf.py` + `llama-quantize` produce a loadable GGUF from a real dockyard checkpoint (needs `DOCKYARD_LLAMA_CPP_DIR`); (b) the dequant guard is built but **not yet wired** — the static GGUF-in-vLLM serve mode (non-refit GGUF load into a deployment engine) is unbuilt; `install_gguf_dequant_guard` must be called at that engine's init and confirmed to reject oversized dims on a live GGUF serve. The export path never invokes vLLM, so #51 is unexercised there. | High (serve) / Med (export) | Run the export against a real checkpoint with llama.cpp tooling and load the GGUF in llama.cpp; when GGUF-in-vLLM serving is built, install the guard at engine init and confirm a served GGUF works and oversized-dim rejection fires. |
| HV-38 | FP8 MoE kernel re-init gate on refit (`models/generation/vllm/quantization/fp8.py::process_weights_after_loading_moe`, #2789) | The patched `Fp8MoEMethod.process_weights_after_loading` re-runs on every weight refit; it now builds the FP8 MoE kernel (`make_fp8_moe_kernel`) only when `self.moe_kernel is None` (initial load), since rebuilding it outside the initial load lacks the `set_current_vllm_config` context and corrupts CUDA-graph MoE state. The `moe_kernel = None` init invariant was confirmed against installed vLLM 0.21 `FusedMoEMethodBase.__init__` (same as the 0.22 pin). No CPU test possible — needs a real FP8 MoE model + CUDA-graph engine + a refit cycle. | Med | On a GPU run serving an FP8 quantized MoE model with CUDA graphs enabled, do a weight refit and confirm the MoE kernel is built once (initial load) and not rebuilt on refit, and generations reflect the synced weights without CUDA-graph corruption. |
| HV-39 | Async GRPO ordered teardown (`algorithms/grpo.py::async_grpo_train` finally block, #2887b) | The async loop's `finally` now shuts down resources in order — environments first (they may hold in-flight HTTP requests to the vLLM endpoints), then generation workers (`policy_generation.shutdown()`), then policy workers (`policy.shutdown()` when distinct from generation) — before "Async GRPO training complete!". Previously only the trajectory collector + replay buffer were killed, leaking generation/policy/env actors. Env handles are `@ray.remote` actors (`environments/*.py`), shut via `ray.get(env.shutdown.remote(), timeout=10)` with a `ray.kill` fallback; the whole block is try/except-guarded so teardown cannot raise. No CPU test — needs a live async cluster (vLLM async engine + env actors). | Med | On an async GRPO run, confirm a normal stop and an error-path stop both tear down environments before generation/policy with no dead-connection retries, and all actors exit cleanly. |
| HV-40 | Tool-call/think-violation graded penalties — live integration (`rewards/invalid_action.py`, `algorithms/reward_functions.py`, `experience/rollouts.py`, `algorithms/grpo.py`, N2/#2800) | The typed/graded violation model, `penalty_mode="auto"` locus routing, reward-locus subtraction, advantage-span overwrite, schema tier, and step-scale are **fully CPU-tested** (`test_invalid_action_penalty.py`, 57: routing, `sample_locus_penalty`, `apply_message_span_advantage_penalties` span-overwrite, graded reward path, rate metrics, schema valid/unknown/bad-args, step-scale). DEFERRED (live RL run): (a) end-to-end on a real rollout — per-message `invalid_action_violations` stamped in both rollout paths flow into the grpo advantage pass (between `compute_advantage` and `_clip_grpo_advantages`) and reward shaping, and the resulting advantages/rewards reflect the routed penalties; (b) **data-plane / non-colocated (TQ) path limitation** — `_dataplane_train_step` strips `message_log` from the driver carry, so it applies all penalties at the **reward** locus (legacy count path) and gets **no** advantage-span penalty — confirm this is acceptable or carry span info through the plane; (c) **N3 registry not yet wired in the rollout** — `assess_assistant_turn` accepts a `ToolRegistry` and validates (tested), but the rollout does not yet pass the env's registry, so the schema tier is dormant until that small follow-up. | Med | On a colocated GRPO run with `invalid_action_penalty.enabled=true` + `penalty_mode=auto`, confirm flagged tool-call/thinking spans get the advantage overwrite, env-semantic invalids subtract from reward, no double-count, and `violation_rate/*` metrics log; then wire + verify the rollout tool registry for the schema tier. |
| HV-41 | Colocated distillation mode (`algorithms/distillation.py` colocated branch + residency helpers, `algorithms/distillation_memory.py`, `models/policy/workers/dtensor_policy_worker.py` async/pinned optimizer offload + `get_model_parameter_count`, `base_policy_worker.py`/`lm_policy.py` `get_total_memory_bytes`, L/#2772) | **CPU-tested** (`test_distillation_colocation.py`, 18): the memory estimator (exact static terms, anti-phase phase peaks `< naive sum`, fast/tight/infeasible selection, forced-schedule errors, safety margin, activation scaling) and the residency orchestration (TIGHT scoring offloads student optimizer + loads teacher; TIGHT training evicts teacher + restores optimizer; FAST does zero transfers but still prepares training; None/separate-cluster only prepares training; resting state TIGHT-offloads / FAST-loads the teacher; schedule round-trips through the config). The **separate-cluster default path is byte-unchanged** (full suite 826 passed/54 skipped). DEFERRED (GPU, ≥1 mesh): (a) **the OOM-safety property itself** — that on a real mesh the per-step peak is `max(scoring, training)` and the anti-phase schedule never co-resides student(weights+optim+grads) with teacher(weights); (b) the preflight estimate accuracy vs real allocator behavior — the `colocation_activation_reserve_gb` default (8.0, teacher charged half) and `colocation_overhead_gb` (2.0) must be **calibrated from observed memory on the first real run**; conservative defaults may refuse a fitting config (raise `colocation_safety_margin`/reserve down) or, if too low, a phase could still OOM (raise them); (c) teacher CPU↔GPU eviction correctness via `offload_after_refit`/`prepare_for_lp_inference` on the frozen no-optimizer teacher; (d) the async/pinned optimizer offload (`move_optimizer_to_device(non_blocking=True)`, reusable pinned buffers for non-DTensor states) transfer correctness + speedup, and the **concurrent side-stream overlap of the offload with teacher compute is NOT implemented** (throughput tuning — the offload synchronizes); (e) the larger `teacher_inference_micro_batch_size` occupancy gain; (f) co-residency at init being bounded to the scoring-phase footprint (student optimizer offloaded before teacher load). | High | On a single-mesh run with `distillation.colocated=true`: (FAST) a small student+teacher fit with zero per-step host↔device transfer and training matches separate-cluster loss; (TIGHT) a larger pair runs with anti-phase eviction, peak GPU memory ≈ max(scoring,training) and never the sum, no OOM across many steps; the preflight refuses an oversized pair at startup with the phase+shortfall message; then calibrate the activation reserve from observed peak and confirm throughput vs separate-cluster (expect ~halved GPU count). |
| HV-42 | Colocated SGLang memory lifecycle + GPU-fraction cap (`models/generation/sglang/sglang_generation.py` `prepare_for_generation`/`finish_generation`, `distributed/worker_groups.py` `colocation_reserved_num_gpus`, `examples/configs/grpo_swe_sglang.yaml`, K/#2898) | **CPU-tested** (`test_sglang_colocation.py`, 10): the GPU-reservation helper (CPU→0, dedicated `max=1`→full GPU **not** capped, colocated `max>1`→`min(0.2,1/max)`, small share below cap preserved) and the lifecycle dispatch (memory-saver on: `prepare_for_generation(tags)`→worker `wake_up(tags=…)` on TP-rank-0, `finish_generation()`→`sleep(tags=None)`, success = `all(r is not False)`, failure on worker-False or `ray.get` raise; memory-saver **off** = full no-op, both hooks return True and fan out nothing — historical always-on behavior byte-unchanged). SGLang is colocated-only (`weight_sync/factory.py` rejects non-colocated). DEFERRED (GPU, shared mesh): (a) **the OOM-relief property** — with `enable_memory_saver: true` the server actually releases GPU during the training phase (so the co-resident trainer can use it) and resumes before generation; verify peak memory reflects the release and a larger model that OOMs always-on now fits; (b) the two-phase HTTP refit resume (`prepare_for_generation(tags=["weights"])` then `["kv_cache"]`) correctly stages weights then rebuilds KV cache via SGLang's `resume_memory_occupation`; (c) startup `finish_generation()` at `init_sglang` idling the server released until first use is harmless on a live server; (d) the `min(0.2,1/max)` reservation lets trainer + inference co-schedule on one bundle without Ray under/over-subscription, and the dedicated path (vLLM `max=1`) still reserves a full GPU unchanged; (e) `release/resume_memory_occupation` tag semantics match SGLang's server build (requires `enable_memory_saver` at launch — set in config). | Med | On a colocated SGLang GRPO run with `enable_memory_saver: true`: confirm GPU memory drops during training (server released) and recovers before generation, the two-tag refit resume works, a model that OOMs with memory-saver off now runs, and Ray schedules trainer+inference on the same bundles; flip memory-saver off and confirm the legacy always-on path is unchanged. |
| HV-43 | ModelOpt NVFP4 real-quant rollout — inference side (`modelopt/models/generation/vllm_modelopt_patch.py`, `modelopt/models/generation/vllm_quant_backend.py` real-quant branches, `vllm_quant_worker.py` `_configure_quant_engine_kwargs`, `modelopt/utils.py` `build_vllm_modelopt_nvfp4_config`/ignore helpers, `models/generation/vllm/config.py` `real_quant`/`real_quant_ignore`/`refit_buffer_size_gb`, `grpo.py` refit_buffer_size_gb wiring, G/#2592) | **CPU-tested** (`test_modelopt_nvfp4_realquant.py`, 16): deployment `quantization_config` builder (NVFP4/w4a16_nvfp4/weight_only markers, default-vs-custom ignore, copied not aliased), quant-ignore matching (lm_head/attention match, mlp.proj does not, prefix+suffix candidate variants), W4A16 detection (`weight_only`/`quant_mode`/nested true; bare `quant_algo`/`nvfp4` false), and the refit param capture/reload roundtrip (capture records shape/dtype/loaders; `prepare_modelopt_for_weight_reload` restores deleted `weight_scale_2` + a mangled `weight` to loaded shape with loaders reattached; intact params untouched; capture idempotent). Faithful port of upstream #2592, version-adapted to vLLM 0.21 (upstream patches the W4A4 `ModelOptNvFp4LinearMethod` + `_from_config` config-attr to force W4A16 via `prepare_fp4_layer_for_marlin`; all imported vLLM symbols verified present in 0.21). Modules import on CPU (vLLM imports are lazy). **Default fakequant path unchanged.** DEFERRED (FP4 hardware — Blackwell/H100 + vLLM runtime): (a) the real NVFP4 W4A16 Marlin GEMM serves correct logits (`apply_modelopt_nvfp4_patches` routes `ModelOptNvFp4LinearMethod` through `apply_fp4_marlin_linear`); (b) end-to-end refit re-pack — IPC/collective `update_weights_*` brackets the stream with `prepare_modelopt_for_weight_reload` → `modelopt_process_weights_after_loading`, weights reload into restored loadable params and reconvert each step; (c) the ignored native-dtype layers (lm_head) copy correctly and their stray scale tensors are dropped in real-quant `_load_weights`; (d) `quant_algo: NVFP4` + `weight_only` actually selects the dense method and the patch's W4A16 branch on the installed build; (e) `refit_buffer_size_gb` sizing for packed NVFP4 chunks. **TRAINER-SIDE PRODUCER NOT BUILT (upstream-blocked):** upstream `DTensorQuantPolicyWorker{,V2}` are `NotImplementedError` stubs ("when modelopt supports AutoModel"); the only working NVFP4 producer is `MegatronQuantPolicyWorker` via `megatron_bridge` (Megatron — excluded by dockyard). So end-to-end real-quant RL refit needs a DTensor NVFP4 export path that neither upstream nor ModelOpt-AutoModel provides yet; the inference side is ready-for-producer. `POLICY_WORKER_OVERRIDES` still points at the absent `dtensor_quant_policy_worker` (pre-existing; only hit if `quant_cfg` is set on the *policy* config — not needed for inference-side real-quant). | High | On FP4 hardware, load a static NVFP4 checkpoint (`scripts/quantize_model.py`) with `generation.real_quant=true`: confirm the real Marlin W4A16 path serves correct logits; then, once a DTensor NVFP4 producer exists, confirm per-step refit reloads packed weights and re-converts with no accuracy drift vs fakequant. |
| HV-44 | MOPD on-policy distillation — core + teacher group (`algorithms/opd.py`, `algorithms/advantage_estimator.py` `OPDAdvantageEstimator` + `grpo.py` registration, `models/policy/teacher_worker_group.py`, H/#2780) | **CPU-tested** (`test_opd_distillation.py`, 13): config helpers (`is_opd_enabled` dict/BaseModel/missing, `is_non_colocated_teachers_enabled` short-circuit), the prev_logprobs guard (`assert_prev_logprobs_available` raises when `force_on_policy_ratio` + no `seq_logprob_error_threshold`; inert otherwise / when OPD off), teacher routing (`resolve_reference_aliases` mapped/default-fallback/strict-raise/no-default-raise, `get_teacher_routing_metrics` unique counts + compression + missing-alias raise), the teacher-config builder (dedup of shared checkpoints, per-alias overrides, unknown-key→`dtensor_cfg_overrides`), and `OPDAdvantageEstimator` (`Â = sg[teacher−student]·mask`, stop-gradient, requires both logprobs, metrics). DTensor-adapted port of #2780 (upstream `TeacherWorkerGroup` is Megatron-only — raises on `dtensor_cfg.enabled`; dockyard wraps an inference-only DTensor `Policy.get_logprobs` instead, mirroring `distillation.py`'s teacher). Loss already supports the OPD requirements (icepop/disable_ppo_ratio/force_on_policy_ratio). **H5 async-GRPO orchestration IS WIRED** (`async_grpo_train` + `async_utils/trajectory_collector.py`): on `is_opd_enabled`, `async_grpo_train` runs `assert_prev_logprobs_available`, creates teacher groups via `create_teacher_worker_groups`, passes them to the `AsyncTrajectoryCollector` actor; the collector's `_score_teacher_logprobs`/`_compute_teacher_logprobs` (CPU-tested for routing, DP-padding, stitching, default-routing) score each rollout batch off the event loop and stamp `teacher_reference_logprobs`; `async_grpo_train`'s advantage call threads `_opd_teacher_logprobs_for_advantage` (reads + `_pad_teacher_logprobs`) into `compute_advantage(teacher_logprobs=…)`; teardown shuts the teacher groups in the finally. Scoped to the in-buffer async path — OPD on the **data-plane** path raises NotImplementedError (guarded), and sync `grpo_train` is unchanged. DEFERRED (GPU/cluster): (a) **TeacherWorkerGroup live** — the inference-only Policy loads a frozen teacher and `get_logprobs` returns correct `[B,S]` logprobs for the on-policy tokens; (b) `create_teacher_worker_groups` dedicated-fleet placement + health-check on a real cluster (NVLink-segment placement layered on by item J — `segment_size=None` for now); (c) **teacher-group serialization into the collector Ray actor** — the TeacherWorkerGroup (wrapping a Policy → RayWorkerGroup) is passed as a `.remote()` kwarg; verify it pickles into the actor (upstream passes its Megatron worker group the same way, but dockyard's Policy wrapper is unverified here); (d) **seq-length alignment** — the collector flattens message_log to `teacher_seq_pad_multiple` while the trainer flattens to `make_sequence_length_divisible_by`; `_pad_teacher_logprobs` right-pads teacher→train_S and raises if teacher_S>train_S — confirm the grids line up on a real run; (e) the ICE-POP loss truncates correctly end-to-end and multi-teacher alias routing + dedup behave on a cluster. | High | On a cluster with `adv_estimator='opd'` + a frozen teacher checkpoint (async, in-buffer, data_plane off): confirm teacher groups come up, the collector stamps teacher_reference_logprobs, `Â = teacher−student` advantages flow into training, ICE-POP truncates, multi-teacher routing/dedup works, and teardown shuts teachers down; check the collector-actor serialization and seq-grid alignment. |
| HV-45 | xtoken cross-tokenizer distillation — GPU producers + multi-teacher driver (M4.c/d + #2797; `models/policy/workers/dtensor_policy_worker_v2.py` `get_full_logits_ipc`, `models/policy/lm_policy.py` `get_full_logits_ipc`, `models/policy/utils.py` `ensure_teacher_ipc_buffer`, `algorithms/xtoken_off_policy_distillation.py`, `examples/run_xtoken_distillation.py`) | **CPU-tested — the entire multi-teacher loss/keystone/collate/config surface.** `CrossTokenizerDistillationLossFn` aggregates a per-teacher KD term (P-KL/gold for a cross-tokenizer teacher, direct top-k KL for a same-tokenizer teacher whose `projection_matrix_path` is null) per `kd_loss_mode` (sum with static or dynamic ce/entropy/max-prob weights, averaged_logits, select_teacher) combined with one student CE; per-teacher `_t{i}` metrics. The keystone rebuilds each teacher over its own transport key (`teacher_{i}_full_logits_ipc` / `teacher_{i}_full_logits_cross_cluster`) into `teacher_full_logits_by_idx` / `aligns_by_idx` (thin identity align for a same-tokenizer teacher); the collator tokenizes the student once + each cross-tokenizer teacher into `teacher_{i}_*` / `alignment_{i}_*`. Full suite 948 passed + 71 xtoken tests (`test_xtoken_loss_fn.py` 16 incl. two-teacher weighted sum, same-tokenizer direct KL, averaged_logits, select_teacher, dynamic ce/entropy/max-prob weights, and the config validators; `test_xtoken_keystone.py` 8 incl. two teachers + a same-tokenizer thin align; `test_xtoken_collate.py` / `test_xtoken_config.py` multi-teacher). Both transport legs reassemble the identical `[B, T_t/CP, V_t]` contract per teacher. **NOT BUILT — the GPU-only per-teacher producers + the multi-teacher driver (M4.c/d), spec in `handoff/m-xtoken-build-plan.md`.** DEFERRED: (a) the per-teacher IPC producer emitting the `teacher_{i}_*` keys; (b) the per-teacher cross-cluster producer over the data_plane; (c) the driver `xtoken_off_policy_distillation.py` building N teacher worker groups, injecting the parallel per-teacher lists + `student_vocab_size` into loss_fn, and running the per-step multi-teacher scoring → `student_policy.train`. | High | On a multi-GPU run with a student + ≥2 teachers of different tokenizers (and a same-tokenizer teacher): confirm each teacher's IPC / cross-cluster export → keystone rebuild → the multi-teacher loss yields a finite decreasing loss with correct grads across all three `kd_loss_mode`s and dynamic weighting; verify the per-teacher metrics, the same-tokenizer direct-KL path, vocab-size injection, and teardown. |
| HV-46 | NVLink-segment topology — live placement + ray.sub resource registration (J/#2924, `distributed/virtual_cluster.py` topology core + `RayVirtualCluster` wiring, `cluster/fleet.py` `FleetSpec.segment_size`/`_segment_constraints`, `ray.sub` topology probe; branch `feat/nvlink-segment-topology`) | **CPU-tested — the entire pure selection/sort + wiring surface** (`test_nvlink_segment_topology.py`, 24): `_parse_topology_from_resources` (domain/topo_rank read + sentinels), `select_segment_nodes` (lowest-topo-domain-first, cross-domain span, the **#2924 exclude-unknown-domain fix**, indivisible/insufficiency raises, incomplete-segment handling), `prepare_segment_topology` (None passthrough, no-topology fallback, per-node `{domain: 0.001}` constraints), `_sort_bundle_indices_by_topology` (no-topology (node_id,gpu_id) fallback, domain-min/topo/gpu order, segment-filter discard of incomplete-segment domains), the `RayVirtualCluster` wiring (`segment_size`/`node_resource_constraints` param storage, `_node_bundle_specs` per-node domain-pin merge + independent dicts, mismatched-length reject), and the `FleetSpec` threading (`segment_size` field + validations + `from_env` `DOCKYARD_SEGMENT_SIZE` parse + `build_cluster(segment_size=None)` legacy path). The `ray.sub` probe JSON was simulated valid in both gated states (off = byte-identical to the original `--resources`; on = augmented with `nvlink_domain_<UUID>`/`topo_rank`), key prefix matches `NVLINK_DOMAIN_PREFIX`, `bash -n` clean. Audit verdict: faithful port of the upstream selection/sort logic (adapted to dockyard's `ResourceInsufficiencyError`); topology lookup key (`bundles_to_node_id`) aligns with `get_ray_cluster_topology`'s NodeID keyspace; `segment_size` is harmlessly ignored when no domain info is present; constraints carry only the domain pin (topo_rank is sort-only). Backward-compatible (all new knobs default None/unset → byte-identical legacy behavior). DEFERRED (cluster-bound, GPU+Slurm): (a) **`get_ray_cluster_topology()` live read** — `ray.nodes()` returns the registered `nvlink_domain_*`/`topo_rank` resources on a real fabric (e.g. GB200 NVL72); (b) **domain-pinned placement-group scheduling** — `RayVirtualCluster` bundles with `{nvlink_domain_X: 0.001}` actually land on the selected NVLink domain and `_get_sorted_bundle_indices` (unified-PG GPU path) reads each bundle's domain/topo_rank via the lightweight actors and orders ranks within-fabric (incomplete-segment bundles dropped); (c) **`ray.sub` topology probe** — `nvidia-smi -q` ClusterUUID + `SLURM_TOPOLOGY_ADDR` parse register the custom resources on each head/worker node (gated on `DOCKYARD_SEGMENT_SIZE`); (d) end-to-end: a fleet built with `DOCKYARD_SEGMENT_SIZE` set places collective groups within one NVLink fabric. Underpins item H's teacher placement (which used `segment_size=None`) and TP-spanning-node inference. J3 ray.sub + fleet threading = this entry; only the live cluster behavior remains. | Med | On a topology-registered cluster (GB200 NVL72 or any node-set with `nvlink_domain_*`/`topo_rank` Ray resources from the ray.sub probe), set `DOCKYARD_SEGMENT_SIZE`: confirm `prepare_segment_topology` selects segment-aligned nodes, the placement groups schedule onto the pinned domains, `_get_sorted_bundle_indices` orders ranks by physical topology (and drops incomplete-segment GPUs), and a TP/collective group lands within one fabric; verify the default (unset) path is unchanged. |
| HV-47 | MoE router-replay (R3) — live capture + multi-rank DTensor replay (item I / #2908; `models/generation/{vllm,sglang}/*` expert-id capture, `experience/rollouts.py` carry, `distributed/batched_data_dict.py`, `models/dtensor/moe/{router,dispatch}.py` replay, `algorithms/grpo.py` setup guard) | CPU-tested: the capture→align leaf, the router hook + replay controller, the policy-worker forward binding, the vLLM expert-id capture, the rollout→train carry + setup guard, and the async / data-plane compat guards (58 CPU tests, incl. the routing-backfill positional-alignment fix an earlier test missed). The full dataflow is wired: generation captures `topk_expert_ids` → carried on the rollout data dict → replayed in the DTensor MoE dispatch under the `router_replay` policy flag. **NOT CPU-verifiable — live vLLM/sglang expert-id emission + multi-rank DTensor deterministic replay.** DEFERRED: (a) the generation backends surfacing routing at scale; (b) multi-rank replay reproducing the recorded dispatch on a real MoE policy. | Med | On a multi-GPU run with an MoE policy + `router_replay` enabled: confirm generation captures the selected expert ids, they carry through to the train batch, the DTensor MoE forward replays the recorded routing, and the recomputed-vs-generation logprob error (`lp_error`) drops relative to a no-replay baseline. |

## Notes
- **Chunked delta-rule kernel (perf, not correctness):** the J11 linear-attention recurrence uses a `lax.scan` (O(S) sequential); the HF chunked kernel is a perf optimization computing identical math (CPU-parity-proven). Implementing/benchmarking the chunked form is a J10/perf task (any GPU), not a correctness HV — correctness is already covered.
- **`apply_mask_to_padding_states` omitted in the JAX linear-attn module (right-padding assumption):** correct for right-padded causal training (padded positions never pollute real ones); would bite only under left-padding. A documented assumption, not a GPU-gated check — the trainer must keep right-padding.
- Resolved-without-hardware (NOT deferred): A.1 #2569 reasoning-parser wiring was hardened
  and verified against a real installed vLLM (0.21, same parser-API line as the 0.22 pin);
  A.3 #2686/#2478 numerics are bit-validated against the original in pure torch.
- The standing path to closing most of these is the single-node un-packed bring-up the
  production-readiness audit calls for (tiny model, packing off, `_v2=false` for the dense
  v1 path), then a small MoE model (Qwen3-MoE / GLM) for the EP-specific items (HV-1/2/8).
